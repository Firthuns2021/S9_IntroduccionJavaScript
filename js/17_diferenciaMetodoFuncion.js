const numero1 = 20;
const numero2 = "20";


console.log( parseInt(numero2) ); // parseInt() es una función
console.log( numero1.toString() ); // .toString() es un método



// Una función es aquel fragmento de código que al llamarse esta ejecuta una acción,
// una función al trabajarse con un objeto puede ejecutar una acción sobre este ,
// es decir funcionar como un método pero no ser un método en si.
//     Por otro lado los métodos decía que eran un conjunto de funcionalidades, o funciones que dependen del objeto.
